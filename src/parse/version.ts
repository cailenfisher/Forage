// Bumped whenever parser logic in parse/*.ts changes meaningfully. Stored
// alongside each artifact so a re-parse can be attributed to the code that
// produced it.
export const PARSER_VERSION = '0.2.0';
